---
# Extra packages for quality of life
modules:
  - type: dnf
    repos:
      cleanup: true
      files:
        - https://pkgs.tailscale.com/stable/fedora/tailscale.repo
    install:
      install-weak-deps: false
      packages:
        # Third-party repos: tailscale
        - tailscale
